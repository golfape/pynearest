from distutils.core import setup
setup(
  name = 'pynearest',
  packages = ['pynearest'], # this must be the same as the name above
  version = '0.1',
  description = 'Pure python online nearest neighbours',
  author = 'GolfApe',
  author_email = 'golfape@gmail.com',
  url = 'https://github.com/golfape/pynearest', # use the URL to the github repo
  download_url = 'https://github.com/golfape/pynearest/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['nearest neighbour', 'nearest','neighbour'], # arbitrary keywords
  classifiers = [],
)